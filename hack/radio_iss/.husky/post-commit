#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

cd hack/radio_iss || exit 0

# IMPORTANT: Do not modify or amend commits here — it creates
# recursive hooks and leaves staged changes after commit/push.

echo "🧪 Verifying deployment..."
chmod +x verify-deployment.sh 2>/dev/null || true
./verify-deployment.sh || true